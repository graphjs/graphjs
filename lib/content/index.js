import english from './english';
import french from './french';

const AllContent = {
    english,
    french
}; 

export default AllContent;
export const ListOfLanguagesAvailable = Object.keys(AllContent);