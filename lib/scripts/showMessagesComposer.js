import _showHelper from './_showHelper.js';
const tagName = 'graphjs-messages-composer';
const hookName = 'ShowMessagesComposer';

export default function(specs) {
	_showHelper(tagName, hookName, specs);
}